PORT=5000

# Option A: Local MongoDB (must be installed and running)
# MONGO_URI=mongodb://localhost:27017/crisisgrid

# Option B: MongoDB Atlas (free tier) - create cluster at https://cloud.mongodb.com
# Replace <password> with your DB user password and <cluster> with your cluster hostname
# MONGO_URI=mongodb+srv://<username>:<password>@<cluster>.mongodb.net/crisisgrid?retryWrites=true&w=majority

MONGO_URI=mongodb://localhost:27017/crisisgrid

JWT_SECRET=your_secret_key_change_in_production
